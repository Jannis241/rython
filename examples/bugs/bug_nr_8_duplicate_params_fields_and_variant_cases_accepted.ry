fn pick(x: int, x: int) int {
    // Bug: duplicate params are accepted; this reads only the second x.
    return x;
}

struct Bad {
    // Bug: duplicate fields are accepted into the IR type definition.
    x: int,
    x: bool
}

variant V {
    // Bug: duplicate variant cases are accepted.
    A,
    A
}

fn main() V {
    return V::A;
}
