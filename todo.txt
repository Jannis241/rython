Informationen für Jesko:
1. Jede TempId, die aus Alloca/GlobalAddr/GetFieldAddr/GetElementAddr kommt,
     ist eine Adresse, also logisch Pointer(...).

  2. Jeder IrType::Named(name) muss genau eine passende IrTypeDefinition
     in IrModule.types haben.


Errors laut codex:

Jesko:
    1. crates/ir_to_assembly/src/codegen.rs:267: Float-Vergleiche (Eq, Ne, Lt, usw.) schreiben das Bool-Ergebnis nach rax, danach
    wird aber immer xmm0 per movsd gespeichert. Dadurch landet bei Float-Comparisons vermutlich der linke Float-Operand statt
    0/1 im Ergebnis-Temp.

    2. crates/rython_to_ir/src/parser.rs:382: Char-Literals können laut Lexer leer sein, aber der Parser macht
    chars.first().unwrap(). Ein leeres Char-Literal kann dadurch paniken statt einen Parser-Fehler zu liefern.

Jannis
    1. crates/rython_to_ir/src/codegen.rs:416: Mehrere return-Statements in einem Block werden weiter generiert, und der letzte
    Return überschreibt den Terminator. Aktuell bedeutet return 1; return 2; effektiv return 2;, statt nach dem ersten Return
    abzubrechen oder unreachable Code sauber zu behandeln.

    2. crates/rython_to_ir/src/codegen.rs:558: void ist als Parameter-Typ erlaubt und wird sogar getestet. Das erzeugt LoadParam/
    Store für einen Wert, den es semantisch nicht geben sollte.



- insgesamat:
    - errors (msg)
    - funktion overloading!?!?!?!?


eigener assembly und linker schreiben?

ir_to_asm:
    //DONEDONEDONE f64 logik komplett implementen
    Struct Literals
    Array Literals
    String Literals
    Variant Literals
    dass man innerhalb innerhalb eines von inline asm werte variable referencen kann außerhalb des asemblais

parser und ast:
    - eigene scopes mit {} erstellen


ir_code_gen:
    Funktion parameter richtig handeln

    Pointer einbauen (codex) für alloc, load, store, globaladdr, getfieldaddr, getelementaddr,
    Type-Tracking für temps

    Mehrere Blöcke machen für if while usw

    globale funktionen??? pub keyword????? wie??????

    constans
    globals
    types:
        Structs
        Variants

    traits auflösen
    types resolven
    operator
    names mangeln (! codex fragen was genau wo gemangelt werden muss weil jesko = kp)

    Variablen
        - Scopes:
            - neue scopes bei: Functions, while, for, loop, if (immer bei '{')
            - eigene scopes mit {}
            - global scope

    functions:
        parameter + generic types
        Expr:
            String, List zu einem Struct machen
            Structs mit init struct instruction handeln
            variant handeln
            BinaryOP
            Unary
            Variables (consts, globs)
            Assign
            Call
            FieldAccess
            Literals
            Grouping
        Stmt:
            Let
            If
            Loop
            While
            For
            Return
            Break,
            Continue,
            Expr,
