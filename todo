

- insgesamat:
    - errors (msg)
    - funktion overloading!?!?!?!?


ir_to_asm:
    //DONEDONEDONE f64 logik komplett implementen
    Struct Literals
    Array Literals
    String Literals
    Variant Literals
    dass man innerhalb innerhalb eines von inline asm werte variable referencen kann außerhalb des asemblais

lexer (wegen jesko ai slop!!!!!!!!!!!!!!!!)
    asm error handling verbessern


parser und ast:
    - eigene scopes mit {} erstellen


ir_code_gen:

    strucutre für alles planen damit jesko besser arbeiten kann:
        - alles umbenenn
        - alle verzeweigten dinge in ir module erklären mit beispiel in ir output
        -

    ir output aussehen für jesko in das file schreiben

    namen von allem verbessern und klarer machen

    jesko load param gucken ob gut (slop)
    gucken ob jesko param handling gut oder (||) slop

    ConstValue:
        - struct, variant string fixxen (löschen)

    gen expr: String, list zu einem struct machen --> init struct instruction???
    gen expr: variant zu einem int machen

    ConstValue zu primitiveValue umbennen (macht mehr sinn)

    informationen bei error return type hinzufügen (zb expected type)

    Variablen
        - Scopes:
            - neue scopes bei: Functions, while, for, loop, if (immer bei '{')
            - eigene scopes mit {}
            - global scope
    functions:
        parameter + generic types
        Expr:
            BinaryOP
            Unary
            Variables (consts, globs)
            Assign
            Call
            FieldAccess
            Literals
            Grouping
        Stmt:
            Let
            If
            Loop
            While
            For
            Return
            Break,
            Continue,
            Expr,
    constans
    globals
    types:
        Structs
        Variants


    traits auflösen
    types resolven
    operator
    names mangeln (! codex fragen was genau wo gemangelt werden muss weil jesko = kp)

