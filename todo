ir_to_asm:
    f64 logik komplett implementen
    Struct Literals
    Array Literals
    String Literals
    Variant Literals
    dass man innerhalb innerhalb eines von inline asm werte variable referencen kann außerhalb des asemblais

lexer (wegen jesko ai slop!!!!!!!!!!!!!!!!)
    asm error handling verbessern


parser und ast:
    - eigene scopes mit {} erstellen


ir_code_gen:
    jesko load param gucken ob gut (slop)
    gucken ob jesko param handling gut oder (||) slop

    informationen bei error return type hinzufügen (zb expected type)

    Variablen
        - Scopes:
            - neue scopes bei: Functions, while, for, loop, if (immer bei '{')
            - eigene scopes mit {}
            - global scope
    functions:
        parameter + generic types
        Expr:
            BinaryOP
            Unary
            Variables (consts, globs)
            Assign
            Call
            FieldAccess
            Literals
            Grouping
        Stmt:
            Let
            If
            Loop
            While
            For
            Return
            Break,
            Continue,
            Expr,
    constans
    globals
    types:
        Structs
        Variants


    traits auflösen
    types resolven
    names mangeln

